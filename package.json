{
  "name": "auto-post-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Automated social-media post scheduler. Monorepo: client (Vite + React), server (Express + PostgreSQL + S3), scripts (automation/cron worker).",
  "workspaces": [
    "client",
    "server",
    "scripts"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,green \"npm run dev -w client\" \"npm run dev -w server\"",
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "start:scripts": "npm run start -w scripts",
    "scheduler": "npm run start -w scripts"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
